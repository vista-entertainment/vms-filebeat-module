type: log
paths:
{{ range $i, $path := .paths }}
 - {{$path}}
{{ end }}
exclude_files: [".gz$"]

multiline:
  pattern: '^([[:space:]]|\t)+at\b'
  negate: false
  match: after
#  flush_pattern: '^[0-9]{4}-[0-9]{2}-[0-9]{2}'

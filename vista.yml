#==================== Elasticsearch Vista setting ==========================
- module: vista
  # Vista logs
  log:
    enabled: true

    # Set custom paths for the log files. If left empty,
    # Filebeat will choose the paths depending on your OS.
    var.paths: ["C:/Vista/Log/*/*.log"]
    var.exclude_files: ["Request*.log"]    